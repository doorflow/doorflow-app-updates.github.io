{
  "schema": 2,
  "apps": [
    {
      "slug": "doorflow-desktop",
      "name": "DoorFlow Desktop",
      "downloads": [
        {
          "platform": "macos",
          "label": "macOS",
          "version": "1.9.9 (23)",
          "url": "https://github.com/doorflow/doorflow-app-updates.github.io/releases/download/doorflow-desktop-v1.9.9/DoorFlowDesktop-Setup.dmg",
          "published": "2026-07-03T18:31:17Z",
          "sha256": "83727ebe5c120ff49dad971f5cebc0e8b69e9ad58171575b029926867516bfe0",
          "size": 18036094
        },
        {
          "platform": "windows",
          "label": "Windows",
          "version": "1.9.10 (24)",
          "url": "https://github.com/doorflow/doorflow-app-updates.github.io/releases/download/doorflow-desktop-v1.9.10/DoorFlowDesktop-Setup.exe",
          "published": "2026-07-06T19:29:32Z",
          "sha256": "4184e05bb5d4982fdbb3df55f027d581c38f47d62eb4c642cf5c53e6d1865084",
          "size": 99519480
        }
      ],
      "notes_html": "<p># DoorFlow Desktop 1.9.10 (24)</p>\n<p>### Fixed</p>\n<ul>\n<li>Relaunching now reopens **all** the dashboard windows you had open, each at its saved size, position and view context (the selected list/group or the template that was being edited, and the selected person) — previously only a single window was restored, with no context.</li>\n<li>If you relaunch with fewer screens than when you saved, windows that no longer fit are gathered onto the main screen, neatly cascaded (rather than being dropped).</li>\n<li>The application-lock password window now appears centred on the same display as the dashboard window you were using, instead of at a fixed position.</li>\n<li>Template editor: switching between templates no longer snaps the edit palette back to the SETTINGS tab — it stays on the tab you were using (the palette still reloads for the newly selected template).</li>\n<li>Card readers are no longer mis-detected as card printers. An HID Global OMNIKEY reader was matching the &quot;HID&quot; card-printer family (which exists to catch HID Fargo printers); reader product lines (OMNIKEY, iCLASS, prox, Signo) are now excluded on both macOS and Windows.</li>\n<li>Template editor: alignment guides now line up exactly with a field&#x27;s right and bottom edges (they previously drifted ~1px because the object&#x27;s far edges and the guide were rounded differently); object edges also render pixel-consistent regardless of position.</li>\n<li>Template editor: a selected field&#x27;s resize handles now sit exactly on the corners/edges (previously biased ~½px, most visible on the right and bottom) and draw on top of the field&#x27;s bounding box instead of being painted over by it.</li>\n</ul>\n<p>### Changed</p>\n<ul>\n<li>Centre- and right-aligned card text now word-wraps to fit the field with each line individually aligned, instead of drifting off to one side when the content is wider than a narrow field.</li>\n<li>Card field values are trimmed of stray leading/trailing whitespace before rendering, so a trailing space in data (e.g. a job title) no longer throws off centre/right alignment.</li>\n</ul>\n<p>### Added</p>\n<ul>\n<li>**Lock Application** menu item (next to Sign In / Sign Out) to lock the app on demand - the same lock as the inactivity policy. Enabled once a lock password has been set.</li>\n<li>Template editor: fields now have a **vertical text alignment** control (top / centre / bottom) alongside the existing horizontal alignment; existing fields keep their current bottom placement.</li>\n<li>Template editor: a selected field&#x27;s corner and edge handles can now be dragged to resize/reshape it (corners resize both axes, edge handles one), with hover cursors and live **alignment guides** that snap a field&#x27;s edge or centre to nearby fields - to the card&#x27;s own centre lines, and to a 30-unit safe margin inside each card edge - as you drag or resize.</li>\n<li>**Application lock:** the app now prompts for the password set during first-run - on launch (before the main window is drawn, so there&#x27;s no flash of the UI), and again when you return to it after 5 minutes away. While locked, all other windows are hidden and then restored to their exact positions on unlock; the lock blocks the interface only, so background sync and printing keep running.</li>\n</ul>"
    }
  ]
}
