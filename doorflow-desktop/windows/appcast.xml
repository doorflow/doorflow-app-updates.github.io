<?xml version='1.0' encoding='utf-8'?>
<rss xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle" version="2.0">
  <channel>
    <title>DoorFlow Desktop updates (win)</title>
    <link>https://updates.doorflow.com/doorflow-desktop/windows/appcast.xml</link>
    <item><title>Version 1.14.14 (102)</title><pubDate>Fri, 07 Aug 2026 21:30:22 +0000</pubDate><description><![CDATA[<!DOCTYPE html>
<style>
html,body{margin:0;padding:0}
body{font-family:"Segoe UI",-apple-system,system-ui,"Helvetica Neue",Arial,sans-serif;
font-size:13px;line-height:1.5;color:#1f1f1f;padding:2px 6px 8px}
h1{font-size:14px;font-weight:600;color:#666;margin:0 0 10px;padding-bottom:6px;
border-bottom:1px solid #e5e5e5}
h2{font-size:14px;font-weight:600;margin:14px 0 4px}
h3{font-size:13px;font-weight:600;margin:14px 0 4px}
p{margin:0 0 8px}
ul{margin:0 0 10px;padding-left:20px}
li{margin:0 0 6px}
code{font-family:Consolas,"Courier New",monospace;font-size:12px;background:#f2f2f2;
padding:1px 4px}
a{color:#1a5fb4;text-decoration:none}
hr{border:0;border-top:1px solid #e5e5e5;margin:14px 0}
</style>
<h1>DoorFlow Desktop 1.14.14 (102)</h1>
<h3>Changed</h3>
<ul>
<li>Minor user-interface tweaks.</li>
</ul>]]></description><enclosure url="https://github.com/doorflow/doorflow-app-updates.github.io/releases/download/doorflow-desktop-v1.14.14/DoorFlowDesktop-Setup.exe" sparkle:version="102" sparkle:shortVersionString="1.14.14" length="100627568" type="application/octet-stream" sparkle:edSignature="ba5MIStOwhijXhfYFPJU4Ygjj1JexnGaKn9w8bJ38TUYXt1ROMzHIdSFICo3nIZn2N+JgVgewzZFapnTXuO5BA==" sparkle:os="windows"/></item><item><title>Version 1.14.12 (99)</title><pubDate>Fri, 07 Aug 2026 21:13:54 +0000</pubDate><description><![CDATA[<!DOCTYPE html>
<style>
html,body{margin:0;padding:0}
body{font-family:"Segoe UI",-apple-system,system-ui,"Helvetica Neue",Arial,sans-serif;
font-size:13px;line-height:1.5;color:#1f1f1f;padding:2px 6px 8px}
h1{font-size:14px;font-weight:600;color:#666;margin:0 0 10px;padding-bottom:6px;
border-bottom:1px solid #e5e5e5}
h2{font-size:14px;font-weight:600;margin:14px 0 4px}
h3{font-size:13px;font-weight:600;margin:14px 0 4px}
p{margin:0 0 8px}
ul{margin:0 0 10px;padding-left:20px}
li{margin:0 0 6px}
code{font-family:Consolas,"Courier New",monospace;font-size:12px;background:#f2f2f2;
padding:1px 4px}
a{color:#1a5fb4;text-decoration:none}
hr{border:0;border-top:1px solid #e5e5e5;margin:14px 0}
</style>
<h1>DoorFlow Desktop 1.14.12 (99)</h1>
<h3>Fixed</h3>
<ul>
<li><strong>Windows update prompt now shows the version, not a build number.</strong> WinSparkle&#x27;s &quot;you have X&quot; line read a build number (e.g. &quot;you have 51&quot;) because the app reported its build number in WinSparkle&#x27;s <em>display</em> slot. It now sets the <strong>display</strong> version (<code>win_sparkle_set_app_details</code>) to the marketing version (e.g. <code>1.14.11</code>) and the <strong>comparison</strong> version (<code>win_sparkle_set_app_build_version</code>) to the build number separately, matching how the appcast carries <code>shortVersionString</code> (display) vs <code>version</code> (compare). <em>Takes effect only once a user is running a build containing this change - installs already in the field keep showing their build number until they update.</em></li>
<li><strong>A people sync of a large account no longer aborts on a single network blip.</strong> A big account is fetched a page at a time (~50 people per page, so ~130 pages for 6,400 people); previously <em>any</em> failed page - most often a transient timeout or dropped connection on a busy/proxied network - abandoned the entire run and discarded all progress, which is why a sync would repeatedly &quot;fail after around 600.&quot; Each page is now <strong>retried up to 4 times with backoff (2/4/8/16s)</strong> before the sync gives up, and the retry budget resets on every successful page, so a network that blips once per page can still complete.</li>
<li><strong>A garbled response, or one bad person record, no longer kills the whole sync.</strong> A page that comes back unparseable (e.g. a proxy returning an HTML error page with a <code>200</code> status) is now <strong>retried</strong> like any other transient page failure instead of failing the sync outright; and a single malformed person record inside an otherwise-valid page is <strong>skipped and logged</strong> rather than abandoning the remaining thousands.</li>
</ul>
<h3>Changed</h3>
<ul>
<li><strong>The card preview no longer re-decodes the photo from disk on every repaint.</strong> During a live window resize the preview repaints continuously, and each paint was re-reading and re-decoding the person&#x27;s photo from disk. The decoded image is now memoised on the person (reloaded if the photo URL changes), so a burst of repaints costs one decode instead of dozens.</li>
<li><strong>Photos now download in parallel instead of one-at-a-time.</strong> The photo cache used to fetch every person&#x27;s images sequentially with a blocking request each - tens of minutes for a large account. It now runs <strong>up to 5 downloads concurrently</strong> on the main event loop (no background thread), which cuts a big account&#x27;s first photo sync from tens of minutes to a few, while staying polite enough not to trip an image host&#x27;s rate-limiting. A stall watchdog aborts a pass only if nothing completes for 60s, so a wedged network can&#x27;t block future passes.</li>
<li><strong>The people and printed-cards lists are snappier on large accounts.</strong> They now render at most the first <strong>200</strong> people / <strong>500</strong> printed cards instead of building a row for every record (thousands of rows was the dominant cost of opening a list, and nobody scrolls that far). <strong>Search is unchanged and still runs against everyone</strong> - this only limits how many rows are drawn, so anyone is still found by typing a name/email. When a list is capped, the <strong>search box placeholder</strong> shows a &quot;Showing first N - type to search all&quot; hint. Selection and card actions operate on the displayed rows as before. (Also fixed an O(n²) array-growth pattern in all three list builders.)</li>
<li><strong>Large-account syncs are substantially faster.</strong> Each page of people is now written to the local database in a <strong>single transaction</strong> instead of one disk commit per person - roughly 50× fewer commits per page - which was the dominant cost of a slow sync on a big account. (Internally this required making the database&#x27;s transactions reentrant, using SQLite savepoints, so a single failing record rolls back just itself rather than the whole page.)</li>
<li><strong>Photo caching now downloads the most-recently-updated people first.</strong> The cache pass is ordered by <code>updated_at</code> (newest first) instead of alphabetically, so freshly-changed and just-added people get their photos before an older backlog - meaning an interrupted or throttled pass still caches the faces a front-desk operator is most likely to need now.</li>
<li><strong>Photo caching stops hammering an image host that is refusing requests.</strong> When a run of image downloads fails back-to-back (an expired signing scheme, or a corporate proxy/firewall returning <code>403</code>), the cache pass now <strong>stops early</strong> instead of re-attempting the whole failing set (hundreds of images) on every pass, indefinitely, while competing with the people sync. It retries again on the next sync. Any successful download resets the counter, so a few individually-stale URLs don&#x27;t trip it.</li>
</ul>]]></description><enclosure url="https://github.com/doorflow/doorflow-app-updates.github.io/releases/download/doorflow-desktop-v1.14.12/DoorFlowDesktop-Setup.exe" sparkle:version="99" sparkle:shortVersionString="1.14.12" length="100619456" type="application/octet-stream" sparkle:edSignature="y9uVElUxhblVt4pwNPO3X+w8JXhH43kO4oG/lsHa0Hex6sXQoVaIdXI+7E4jpUZAYD7Ss6GZtZC9Iv13QcRbBA==" sparkle:os="windows"/></item><item><title>Version 1.14.11 (97)</title><pubDate>Fri, 07 Aug 2026 20:19:31 +0000</pubDate><description><![CDATA[<!DOCTYPE html>
<style>
html,body{margin:0;padding:0}
body{font-family:"Segoe UI",-apple-system,system-ui,"Helvetica Neue",Arial,sans-serif;
font-size:13px;line-height:1.5;color:#1f1f1f;padding:2px 6px 8px}
h1{font-size:14px;font-weight:600;color:#666;margin:0 0 10px;padding-bottom:6px;
border-bottom:1px solid #e5e5e5}
h2{font-size:14px;font-weight:600;margin:14px 0 4px}
h3{font-size:13px;font-weight:600;margin:14px 0 4px}
p{margin:0 0 8px}
ul{margin:0 0 10px;padding-left:20px}
li{margin:0 0 6px}
code{font-family:Consolas,"Courier New",monospace;font-size:12px;background:#f2f2f2;
padding:1px 4px}
a{color:#1a5fb4;text-decoration:none}
hr{border:0;border-top:1px solid #e5e5e5;margin:14px 0}
</style>
<h1>DoorFlow Desktop 1.14.11 (97)</h1>
<h3>Fixed</h3>
<ul>
<li><strong>A people sync of a large account no longer aborts on a single network blip.</strong> A big account is fetched a page at a time (~50 people per page, so ~130 pages for 6,400 people); previously <em>any</em> failed page - most often a transient timeout or dropped connection on a busy/proxied network - abandoned the entire run and discarded all progress, which is why a sync would repeatedly &quot;fail after around 600.&quot; Each page is now <strong>retried up to 4 times with backoff (2/4/8/16s)</strong> before the sync gives up, and the retry budget resets on every successful page, so a network that blips once per page can still complete.</li>
<li><strong>A garbled response, or one bad person record, no longer kills the whole sync.</strong> A page that comes back unparseable (e.g. a proxy returning an HTML error page with a <code>200</code> status) is now <strong>retried</strong> like any other transient page failure instead of failing the sync outright; and a single malformed person record inside an otherwise-valid page is <strong>skipped and logged</strong> rather than abandoning the remaining thousands.</li>
</ul>
<h3>Changed</h3>
<ul>
<li><strong>The people and printed-cards lists are snappier on large accounts.</strong> They now render at most the first <strong>200</strong> people / <strong>500</strong> printed cards instead of building a row for every record (thousands of rows was the dominant cost of opening a list, and nobody scrolls that far). <strong>Search is unchanged and still runs against everyone</strong> - this only limits how many rows are drawn, so anyone is still found by typing a name/email. When a list is capped, the <strong>search box placeholder</strong> shows a &quot;Showing first N - type to search all&quot; hint. Selection and card actions operate on the displayed rows as before. (Also fixed an O(n²) array-growth pattern in all three list builders.)</li>
<li><strong>Large-account syncs are substantially faster.</strong> Each page of people is now written to the local database in a <strong>single transaction</strong> instead of one disk commit per person - roughly 50× fewer commits per page - which was the dominant cost of a slow sync on a big account. (Internally this required making the database&#x27;s transactions reentrant, using SQLite savepoints, so a single failing record rolls back just itself rather than the whole page.)</li>
<li><strong>Photo caching now downloads the most-recently-updated people first.</strong> The cache pass is ordered by <code>updated_at</code> (newest first) instead of alphabetically, so freshly-changed and just-added people get their photos before an older backlog - meaning an interrupted or throttled pass still caches the faces a front-desk operator is most likely to need now.</li>
<li><strong>Photo caching stops hammering an image host that is refusing requests.</strong> When a run of image downloads fails back-to-back (an expired signing scheme, or a corporate proxy/firewall returning <code>403</code>), the cache pass now <strong>stops early</strong> instead of re-attempting the whole failing set (hundreds of images) on every pass, indefinitely, while competing with the people sync. It retries again on the next sync. Any successful download resets the counter, so a few individually-stale URLs don&#x27;t trip it.</li>
</ul>]]></description><enclosure url="https://github.com/doorflow/doorflow-app-updates.github.io/releases/download/doorflow-desktop-v1.14.11/DoorFlowDesktop-Setup.exe" sparkle:version="97" sparkle:shortVersionString="1.14.11" length="100617016" type="application/octet-stream" sparkle:edSignature="bKrjk8+Jn+aG3g/yp+qhEXoDJTMiCsu59krSNQRHQ6mBAkAn5Mkri4YFaPBKLJ7vIivqnmKtw5PsyLCYgxcIDA==" sparkle:os="windows"/></item><item><title>Version 1.14.9 (95)</title><pubDate>Fri, 07 Aug 2026 17:19:37 +0000</pubDate><description><![CDATA[<!DOCTYPE html>
<style>
html,body{margin:0;padding:0}
body{font-family:"Segoe UI",-apple-system,system-ui,"Helvetica Neue",Arial,sans-serif;
font-size:13px;line-height:1.5;color:#1f1f1f;padding:2px 6px 8px}
h1{font-size:14px;font-weight:600;color:#666;margin:0 0 10px;padding-bottom:6px;
border-bottom:1px solid #e5e5e5}
h2{font-size:14px;font-weight:600;margin:14px 0 4px}
h3{font-size:13px;font-weight:600;margin:14px 0 4px}
p{margin:0 0 8px}
ul{margin:0 0 10px;padding-left:20px}
li{margin:0 0 6px}
code{font-family:Consolas,"Courier New",monospace;font-size:12px;background:#f2f2f2;
padding:1px 4px}
a{color:#1a5fb4;text-decoration:none}
hr{border:0;border-top:1px solid #e5e5e5;margin:14px 0}
</style>
<h1>DoorFlow Desktop 1.14.9 (95)</h1>
<h3>Fixed</h3>
<ul>
<li><strong>A people sync of a large account no longer aborts on a single network blip.</strong> A big account is fetched a page at a time (~50 people per page, so ~130 pages for 6,400 people); previously <em>any</em> failed page - most often a transient timeout or dropped connection on a busy/proxied network - abandoned the entire run and discarded all progress, which is why a sync would repeatedly &quot;fail after around 600.&quot; Each page is now <strong>retried up to 4 times with backoff (2/4/8/16s)</strong> before the sync gives up, and the retry budget resets on every successful page, so a network that blips once per page can still complete.</li>
<li><strong>A garbled response, or one bad person record, no longer kills the whole sync.</strong> A page that comes back unparseable (e.g. a proxy returning an HTML error page with a <code>200</code> status) is now <strong>retried</strong> like any other transient page failure instead of failing the sync outright; and a single malformed person record inside an otherwise-valid page is <strong>skipped and logged</strong> rather than abandoning the remaining thousands.</li>
</ul>
<h3>Changed</h3>
<ul>
<li><strong>The people and printed-cards lists are snappier on large accounts.</strong> They now render at most the first <strong>200</strong> people / <strong>500</strong> printed cards instead of building a row for every record (thousands of rows was the dominant cost of opening a list, and nobody scrolls that far). <strong>Search is unchanged and still runs against everyone</strong> - this only limits how many rows are drawn, so anyone is still found by typing a name/email. When a list is capped, the <strong>search box placeholder</strong> shows a &quot;Showing first N - type to search all&quot; hint. Selection and card actions operate on the displayed rows as before. (Also fixed an O(n²) array-growth pattern in all three list builders.)</li>
<li><strong>Large-account syncs are substantially faster.</strong> Each page of people is now written to the local database in a <strong>single transaction</strong> instead of one disk commit per person - roughly 50× fewer commits per page - which was the dominant cost of a slow sync on a big account. (Internally this required making the database&#x27;s transactions reentrant, using SQLite savepoints, so a single failing record rolls back just itself rather than the whole page.)</li>
<li><strong>Photo caching now downloads the most-recently-updated people first.</strong> The cache pass is ordered by <code>updated_at</code> (newest first) instead of alphabetically, so freshly-changed and just-added people get their photos before an older backlog - meaning an interrupted or throttled pass still caches the faces a front-desk operator is most likely to need now.</li>
<li><strong>Photo caching stops hammering an image host that is refusing requests.</strong> When a run of image downloads fails back-to-back (an expired signing scheme, or a corporate proxy/firewall returning <code>403</code>), the cache pass now <strong>stops early</strong> instead of re-attempting the whole failing set (hundreds of images) on every pass, indefinitely, while competing with the people sync. It retries again on the next sync. Any successful download resets the counter, so a few individually-stale URLs don&#x27;t trip it.</li>
</ul>]]></description><enclosure url="https://github.com/doorflow/doorflow-app-updates.github.io/releases/download/doorflow-desktop-v1.14.9/DoorFlowDesktop-Setup.exe" sparkle:version="95" sparkle:shortVersionString="1.14.9" length="100601000" type="application/octet-stream" sparkle:edSignature="KmvAkiZtTJ2vn5rtCt5hV+Yzgp6eOmw85ogStPiHg8wzNJeDrC8rIBbfzNnhBd3BmBDi0FsarjMszUIBbYacBg==" sparkle:os="windows"/></item><item><title>Version 1.12.2 (80)</title><pubDate>Tue, 04 Aug 2026 11:26:12 +0000</pubDate><description><![CDATA[<!DOCTYPE html>
<style>
html,body{margin:0;padding:0}
body{font-family:"Segoe UI",-apple-system,system-ui,"Helvetica Neue",Arial,sans-serif;
font-size:13px;line-height:1.5;color:#1f1f1f;padding:2px 6px 8px}
h1{font-size:14px;font-weight:600;color:#666;margin:0 0 10px;padding-bottom:6px;
border-bottom:1px solid #e5e5e5}
h2{font-size:14px;font-weight:600;margin:14px 0 4px}
h3{font-size:13px;font-weight:600;margin:14px 0 4px}
p{margin:0 0 8px}
ul{margin:0 0 10px;padding-left:20px}
li{margin:0 0 6px}
code{font-family:Consolas,"Courier New",monospace;font-size:12px;background:#f2f2f2;
padding:1px 4px}
a{color:#1a5fb4;text-decoration:none}
hr{border:0;border-top:1px solid #e5e5e5;margin:14px 0}
</style>
<h1>DoorFlow Desktop 1.12.2 (80)</h1>
<h3>Security</h3>
<ul>
<li>The support <strong>factory reset</strong> (hold Option/Alt on the lock screen) now also clears the <strong>cached person photos and template-background images</strong>, not just tokens, prefs, and the database. Previously a reset performed to move a machine to a different DoorFlow account left the previous account&#x27;s face photos on disk. The image folders are recreated empty on the next launch.</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Exporting diagnostics as a <code>.zip</code> failed on Windows because the shell timed out during PowerShell&#x27;s cold start (before the tiny archive was written). The shell timeout is now generous enough for PowerShell to run; the command was hardened (<code>powershell.exe -ExecutionPolicy Bypass</code>, success judged by whether the archive actually exists rather than PowerShell&#x27;s unreliable exit code); and if zipping still fails it <strong>falls back to a plain <code>.txt</code></strong> so the export always produces a file.</li>
<li>Windows: the <strong>&quot;Template:&quot;</strong> caption above the card preview no longer shows an opaque white box. It was a label layered over the preview canvas, and a transparent label over a canvas doesn&#x27;t render transparently on Windows - so it&#x27;s now drawn directly onto the canvas instead.</li>
</ul>]]></description><enclosure url="https://github.com/doorflow/doorflow-app-updates.github.io/releases/download/doorflow-desktop-v1.12.2/DoorFlowDesktop-Setup.exe" sparkle:version="80" sparkle:shortVersionString="1.12.2" length="100608784" type="application/octet-stream" sparkle:edSignature="lWA8dm/0j5mqplqobaoSx+qElmoAjLBZ/tG19ZAFl7H1okttJJPxZPqUAEpnldpCVcG61xUCQE/BiysV7DCuBg==" sparkle:os="windows"/></item><item><title>Version 1.11.2 (75)</title><pubDate>Tue, 04 Aug 2026 10:56:56 +0000</pubDate><description><![CDATA[<!DOCTYPE html>
<style>
html,body{margin:0;padding:0}
body{font-family:"Segoe UI",-apple-system,system-ui,"Helvetica Neue",Arial,sans-serif;
font-size:13px;line-height:1.5;color:#1f1f1f;padding:2px 6px 8px}
h1{font-size:14px;font-weight:600;color:#666;margin:0 0 10px;padding-bottom:6px;
border-bottom:1px solid #e5e5e5}
h2{font-size:14px;font-weight:600;margin:14px 0 4px}
h3{font-size:13px;font-weight:600;margin:14px 0 4px}
p{margin:0 0 8px}
ul{margin:0 0 10px;padding-left:20px}
li{margin:0 0 6px}
code{font-family:Consolas,"Courier New",monospace;font-size:12px;background:#f2f2f2;
padding:1px 4px}
a{color:#1a5fb4;text-decoration:none}
hr{border:0;border-top:1px solid #e5e5e5;margin:14px 0}
</style>
<h1>DoorFlow Desktop 1.11.2 (75)</h1>
<h3>Added</h3>
<ul>
<li><strong>Connection Doctor</strong> (<strong>Window</strong> menu) - a live checker that actively probes and shows, with green/red status, whether the app can reach the Internet, the <strong>DoorFlow service</strong>, and the <strong>photo (image) store</strong>, plus whether you&#x27;re signed in and when people last synced. It calls out the exact fix in plain English - most usefully, it distinguishes &quot;DoorFlow reachable but the photo host is blocked&quot; (a firewall/proxy problem that lets names and job titles through but stops photos downloading) from being offline or signed out. It <strong>updates on its own</strong> if connectivity changes while it&#x27;s open (e.g. the internet comes back), and has <strong>Sign In Again</strong>, <strong>Export Diagnostics…</strong>, and <strong>Re-check</strong> buttons.</li>
<li><strong>Export Diagnostics</strong> (a button in the Connection Doctor) opens a Save dialog and writes a redacted diagnostic snapshot the customer can email to support, saved as a <strong><code>.zip</code></strong> (which is opaque to content-inspection email gateways, so the attachment - full of names, emails and token-shaped strings - isn&#x27;t flagged or stripped in transit); the saved file is then <strong>revealed in Finder/Explorer</strong> so it&#x27;s easy to find. It&#x27;s named after the app with a timestamp (e.g. <code>DoorFlow-Desktop-Diagnostics-2026-08-03-1430.zip</code>). It records the app version + executable path, sign-in state and key event timestamps (token issue/expiry, last sync, last API error), device <strong>clock/skew</strong> (a clock well off real time can break sign-in), Internet / API-host / image-host reachability, local-data health that separates the two real failure modes for missing photos/job titles (photo URL present but not yet cached = a photo-download/network problem, versus no photo URL or job title at all = a data problem), and a <strong>Recent activity</strong> log of the session&#x27;s key events (sync phases, API errors, sign-in changes, and <strong>photo-download failures with their HTTP status</strong> - the usual smoking gun). Tokens are redacted.</li>
<li><strong>Activity log</strong> - the app now keeps an in-memory log of the session&#x27;s important events (app start, sign-in changes, sync phases and failures, API errors, and photo-download outcomes including the HTTP status of any that fail). It costs nothing on the running app (memory only, no disk writes) and is included in an Export Diagnostics file, so a &quot;sync failed&quot; can be traced to exactly what went wrong.</li>
<li><strong>About DoorFlow Desktop</strong> window showing the app name, the built version (marketing version + build number), the environment/channel, and copyright. It&#x27;s reached from the <strong>Application menu</strong> on macOS (&quot;About DoorFlow Desktop&quot;), from both the card-printing and Front Desk menu bars.</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Signing in now always shows the DoorFlow <strong>login screen</strong> instead of silently reusing an existing browser session, so you can choose which account to sign in with (and aren&#x27;t quietly returned to the wrong one).</li>
<li>A failed sync now explains <strong>what</strong> went wrong instead of a bare &quot;Sync failed&quot;: &quot;No internet connection - can&#x27;t sync&quot; when offline, &quot;Can&#x27;t reach DoorFlow - check your connection&quot; when the service is unreachable, or &quot;Sync couldn&#x27;t complete - some information may be out of date&quot; otherwise. (&quot;Sync failed&quot; read like an app fault when the usual cause is simply no connection.)</li>
<li><strong>Check for Updates…</strong> has moved from the Help menu into the <strong>Application menu</strong> (macOS), next to About. The Help menu (which only held that one item) has been removed from both menu bars.</li>
<li>Template editor - dragging a field from the fields picker onto the card is smoother: (1) a plain click now only <em>selects</em> the field - the drag begins only once you actually move the pointer (a small threshold), rather than on selection; (2) the drag shows a clean translucent (~25% <strong>white</strong>) &quot;ghost&quot; of the field at its real drop size and corner radius, with the field name centred in <strong>uppercase</strong> (not bold); (3) the field is grabbed and dropped <strong>centred on the pointer</strong> instead of snapping its top-left to the cursor; and (4) the previewed and dropped objects come from one shared default-object definition (which also fixes the barcode field&#x27;s size differing between drag and drop). <em>(Drag images render at @1× even on HiDPI - a Xojo framework limitation - so the ghost can look slightly soft on Retina.)</em></li>
</ul>
<h3>Security</h3>
<ul>
<li>The app now <strong>refuses a sign-in with a different DoorFlow account</strong> than the one it&#x27;s already set up for, so one account&#x27;s local people, photos and job titles can&#x27;t mix with - or leak to - another account on the same computer. (Proper account switching is planned.)</li>
<li>Builds now carry a <strong>6-month expiry</strong> (build date + 6 months, stamped at build time). Once a shipped build passes its expiry it stops working and shows an &quot;out of date&quot; screen directing the user to <strong>updates.doorflow.com</strong> to download the latest version - a safety net so very old, potentially-insecure builds don&#x27;t stay in service indefinitely. Debug/IDE runs are never affected, and a mis-stamped/blank expiry fails open (never blocks).</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Cancelling or closing the sign-in window on macOS no longer shows a raw system error (e.g. <em>&quot;…WebAuthenticationSession error 1.&quot;</em>). Cancelling is now silent, and a genuine sign-in problem shows a plain &quot;Sign-in didn&#x27;t complete. Please try again.&quot;</li>
<li>When you&#x27;re not signed in, the bottom-left status now shows a short, clickable <strong>&quot;Sign in…&quot;</strong> link that starts sign-in, instead of a longer message that could crop unreadably in the fixed-width status area (seen on Windows).</li>
<li>The card-printing window&#x27;s title bar showed the old <strong>&quot;DoorFlow CardPrint&quot;</strong> name; it now reads <strong>&quot;DoorFlow Desktop&quot;</strong>.</li>
<li>Person photos now update correctly when someone&#x27;s photo changes in DoorFlow. Previously a changed photo could be missed (the app only checked whether <em>a</em> file was cached, not whether it matched the current photo), and a re-download left the old image behind as an orphaned file. The photo cache now re-downloads when the photo URL changes, deletes the superseded file, records which URL each cached file corresponds to (so it stops re-checking the same people every sync), and refreshes the on-screen photo. Two people who happen to share an identical photo (e.g. the same company logo) no longer overwrite each other&#x27;s cached image - local photo files are now namespaced per person. The diagnostics photo-cache summary now reports people-to-check / downloaded / already-on-disk / failed.</li>
<li>Template editor: a background sync completing while you were editing a template no longer snaps the editor palette back to its first (Settings) tab. The tab you&#x27;re on is now read from the live tab state rather than a variable that only updated when navigating via code - so clicking the Fields/Info tab directly is remembered, and the post-sync template reload restores it correctly.</li>
<li>Windows auto-updater no longer offers the version you already have as if it were a newer update. It now compares the running build against the appcast&#x27;s build number (how the feed is published) rather than the marketing version string, so an up-to-date install correctly reports no update available.</li>
<li>The app now always renders in a light appearance instead of following the system dark-mode setting. On a dark-mode Windows machine the UI (most visibly the Front Desk terminal) had gone almost entirely dark and hard to read, because pure-white backgrounds and pure-black text auto-swap to the system&#x27;s dark colours when dark-mode support is on. Dark-mode support is now off app-wide (<code>Supports Dark Mode</code> build setting), giving a consistent light UI on both platforms.</li>
</ul>]]></description><enclosure url="https://github.com/doorflow/doorflow-app-updates.github.io/releases/download/doorflow-desktop-v1.11.2/DoorFlowDesktop-Setup.exe" sparkle:version="75" sparkle:shortVersionString="1.11.2" length="100608720" type="application/octet-stream" sparkle:edSignature="raIL4Ib1C9J41HF7LK9qTDiKzNZ9hw2CFSoXROiJu7h4AwlD1TLk053ppNlDlN8V9dO7vlo6jHtgj38scUJZBw==" sparkle:os="windows"/></item><item><title>Version 1.9.30 (68)</title><pubDate>Fri, 24 Jul 2026 14:05:34 +0000</pubDate><enclosure url="https://github.com/doorflow/doorflow-app-updates.github.io/releases/download/doorflow-desktop-v1.9.30/DoorFlowDesktop-Setup.exe" sparkle:version="68" sparkle:shortVersionString="1.9.30" length="100532200" type="application/octet-stream" sparkle:edSignature="qLzSiBkEhgF6d7jJR7EHsSwLlFxAVNoIz5o7bQ8RNbQsL9NVJREBvOjrQRJrt2xZBbUYBr3ueEorHB43Ny94Dw==" sparkle:os="windows"/></item><item><title>Version 1.9.23 (51)</title><pubDate>Fri, 24 Jul 2026 11:12:14 +0000</pubDate><enclosure url="https://github.com/doorflow/doorflow-app-updates.github.io/releases/download/doorflow-desktop-v1.9.23/DoorFlowDesktop-Setup.exe" sparkle:version="51" sparkle:shortVersionString="1.9.23" length="100515952" type="application/octet-stream" sparkle:edSignature="bklW5jOVSvI1U8CCZ3KJN1o5G2bj1bX3+PKkp7coPZ3xKwq+Z8bDuuTcJAVoHrj2M7PzGALJAy7PEKWhAKGqBQ==" sparkle:os="windows"/></item><item><title>Version 1.9.22 (48)</title><pubDate>Fri, 24 Jul 2026 10:38:57 +0000</pubDate><enclosure url="https://github.com/doorflow/doorflow-app-updates.github.io/releases/download/doorflow-desktop-v1.9.22/DoorFlowDesktop-Setup.exe" sparkle:version="48" sparkle:shortVersionString="1.9.22" length="100517824" type="application/octet-stream" sparkle:edSignature="1HKxn0HV68pz+OelN/MPIABzZpb8E7gqnCrAeF0axyhwYBu6i+hKfQKCHx5aryFPcCVvD9Og+udol1bdXV/0BQ==" sparkle:os="windows"/></item><item><title>Version 1.9.20 (43)</title><pubDate>Fri, 24 Jul 2026 10:06:29 +0000</pubDate><enclosure url="https://github.com/doorflow/doorflow-app-updates.github.io/releases/download/doorflow-desktop-v1.9.20/DoorFlowDesktop-Setup.exe" sparkle:version="43" sparkle:shortVersionString="1.9.20" length="100529696" type="application/octet-stream" sparkle:edSignature="ZPKGa7ouh+wiA8heyKB/EV6iNQw3rdt2EmvfixGjS1z43XksI7fyX6PmNE/TKwZ7sCTGNzCVo8T092dZkiayAg==" sparkle:os="windows"/></item><item><title>Version 1.9.19 (39)</title><pubDate>Fri, 24 Jul 2026 07:56:22 +0000</pubDate><enclosure url="https://github.com/doorflow/doorflow-app-updates.github.io/releases/download/doorflow-desktop-v1.9.19/DoorFlowDesktop-Setup.exe" sparkle:version="39" sparkle:shortVersionString="1.9.19" length="100527096" type="application/octet-stream" sparkle:edSignature="zYwB1Hurc2eQ7eYOfVlq5Nf9WJ7R/folSLVqI5ejK38Oqx4zF7dr4g3kmN5skluAOlVwbKphkOUu5kflkXKsBQ==" sparkle:os="windows"/></item><item><title>Version 1.9.17 (36)</title><pubDate>Fri, 24 Jul 2026 07:20:14 +0000</pubDate><enclosure url="https://github.com/doorflow/doorflow-app-updates.github.io/releases/download/doorflow-desktop-v1.9.17/DoorFlowDesktop-Setup.exe" sparkle:version="36" sparkle:shortVersionString="1.9.17" length="100533536" type="application/octet-stream" sparkle:edSignature="JpBwDCCNSVEDwlOYwU+Dem9FB+QN2QHvdaTSxCE8xjsuJnCNx+A+cLRvTKuqMkNolEaR5RyOaJA7eDEJ1p13CA==" sparkle:os="windows"/></item><item><title>Version 1.9.15 (33)</title><pubDate>Fri, 24 Jul 2026 06:17:20 +0000</pubDate><enclosure url="https://github.com/doorflow/doorflow-app-updates.github.io/releases/download/doorflow-desktop-v1.9.15/DoorFlowDesktop-Setup.exe" sparkle:version="33" sparkle:shortVersionString="1.9.15" length="100516864" type="application/octet-stream" sparkle:edSignature="Ja+NDpCqSExS1EEBE8TKoct7yMljTxDXC+CoL+C4HPdlYfD8ZR5mvg5ByqFgkTEXjOqF9WDJ0KDS1xrfhovMBw==" sparkle:os="windows"/></item><item><title>Version 1.9.14 (31)</title><pubDate>Thu, 23 Jul 2026 23:16:31 +0000</pubDate><description><![CDATA[<!DOCTYPE html>
<style>
html,body{margin:0;padding:0}
body{font-family:"Segoe UI",-apple-system,system-ui,"Helvetica Neue",Arial,sans-serif;
font-size:13px;line-height:1.5;color:#1f1f1f;padding:2px 6px 8px}
h1{font-size:14px;font-weight:600;color:#666;margin:0 0 10px;padding-bottom:6px;
border-bottom:1px solid #e5e5e5}
h2{font-size:14px;font-weight:600;margin:14px 0 4px}
h3{font-size:13px;font-weight:600;margin:14px 0 4px}
p{margin:0 0 8px}
ul{margin:0 0 10px;padding-left:20px}
li{margin:0 0 6px}
code{font-family:Consolas,"Courier New",monospace;font-size:12px;background:#f2f2f2;
padding:1px 4px}
a{color:#1a5fb4;text-decoration:none}
hr{border:0;border-top:1px solid #e5e5e5;margin:14px 0}
</style>
<h1>DoorFlow Desktop 1.9.14 (31)</h1>
<h3>Fixed</h3>
<ul>
<li>Searching a people/cards list that matches no-one no longer strands you: previously an empty result switched to the empty-state panel, which hid the search field so you couldn&#x27;t edit or clear your search. A search that returns nothing now keeps the list panel (and the search field) visible, and the card preview shows a friendly &quot;no-one selected&quot; empty state (the same copy as the full empty page) instead of the previously-selected person&#x27;s card, with the Template chooser hidden. The full empty-state panel only shows when the view is genuinely empty (no active search).</li>
<li>Person photo on the card preview no longer shifts/crops incorrectly (appearing pushed to the left, and moving as you resize the window). The photo&#x27;s crop was being computed from the on-screen frame size instead of the photo&#x27;s own pixels, so it drifted with the canvas scale and varied by photo; it&#x27;s now a stable centred cover-fit. This also makes the printed-card photo framing consistent.</li>
<li>Incremental (2-minute) sync is now anchored to the DoorFlow server&#x27;s clock from the moment the app launches, closing a window where a device whose clock ran fast could ask the server for changes &quot;since&quot; a time in the future and silently skip recently-updated people. The server-time offset is now measured from the reachability probe (so it&#x27;s known on launch and refreshed every minute) and remembered between launches, instead of resetting until the first data sync of the session.</li>
</ul>
<h3>Added</h3>
<ul>
<li>Template editor: colours (text / border / fill) are now chosen with a built-in colour picker (saturation/value square, hue strip, hex entry, a brightness slider flanked by white/black shortcut swatches) that opens next to the colour square and updates the field and card <strong>live</strong> as you drag - replacing the modal system picker that only applied the colour after you closed it.</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Template editor: an object snapped to a centre guide (the card centre or another object&#x27;s centre) now sits exactly under the guide line - the line is drawn through the object&#x27;s rendered centre, removing the ~1px gap that pixel-rounding otherwise left.</li>
<li>Template editor: alignment guides no longer flicker between two positions when you hold an object near a snap point - the snap now sticks to its chosen target instead of flipping between near-tied candidates.</li>
<li>Completing first-run setup now reliably sticks on macOS. CFPreferences batches its disk flush and could drop the completion flag if you quit soon after finishing, restarting the whole wizard; the flag is now also backed by a small marker file that persists immediately, and the marker is retired once the native setting confirms.</li>
<li>First-run setup is now marked complete as soon as you sign in (rather than only on the final &quot;Open DoorFlow Desktop&quot; button), so quitting during or right after the initial sync no longer restarts the whole first-run wizard on the next launch.</li>
<li>Deleting the template you&#x27;re editing now moves the editor to the next remaining template (instead of leaving the deleted design on the canvas); if no templates remain, it drops to the All People view.</li>
<li>Creating a new card template no longer opens to a blank card - a new template is given a valid default orientation, so it renders immediately instead of only after toggling the front/back orientation.</li>
</ul>]]></description><enclosure url="https://github.com/doorflow/doorflow-app-updates.github.io/releases/download/doorflow-desktop-v1.9.14/DoorFlow%20Desktop-1.9.14%20%2831%29-Setup.exe" sparkle:version="31" sparkle:shortVersionString="1.9.14" length="100500968" type="application/octet-stream" sparkle:edSignature="Eyb5dobe8py7RIv598Wyk6JkkfEnxRAK6pVd9zQM+xV2970g6PJgXfZ0E1hOmhtBN0+/t22lvVGTzzB2aUiLBQ==" sparkle:os="windows"/></item><item><title>Version 1.9.12 (28)</title><pubDate>Wed, 08 Jul 2026 18:21:29 +0000</pubDate><description><![CDATA[<!DOCTYPE html>
<style>
html,body{margin:0;padding:0}
body{font-family:"Segoe UI",-apple-system,system-ui,"Helvetica Neue",Arial,sans-serif;
font-size:13px;line-height:1.5;color:#1f1f1f;padding:2px 6px 8px}
h1{font-size:14px;font-weight:600;color:#666;margin:0 0 10px;padding-bottom:6px;
border-bottom:1px solid #e5e5e5}
h2{font-size:14px;font-weight:600;margin:14px 0 4px}
h3{font-size:13px;font-weight:600;margin:14px 0 4px}
p{margin:0 0 8px}
ul{margin:0 0 10px;padding-left:20px}
li{margin:0 0 6px}
code{font-family:Consolas,"Courier New",monospace;font-size:12px;background:#f2f2f2;
padding:1px 4px}
a{color:#1a5fb4;text-decoration:none}
hr{border:0;border-top:1px solid #e5e5e5;margin:14px 0}
</style>
<h1>DoorFlow Desktop 1.9.12 (28)</h1>
<h3>Added</h3>
<ul>
<li>Template editor: colours (text / border / fill) are now chosen with a built-in colour picker (saturation/value square, hue strip, hex entry, a brightness slider flanked by white/black shortcut swatches) that opens next to the colour square and updates the field and card <strong>live</strong> as you drag - replacing the modal system picker that only applied the colour after you closed it.</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Template editor: an object snapped to a centre guide (the card centre or another object&#x27;s centre) now sits exactly under the guide line - the line is drawn through the object&#x27;s rendered centre, removing the ~1px gap that pixel-rounding otherwise left.</li>
<li>Template editor: alignment guides no longer flicker between two positions when you hold an object near a snap point - the snap now sticks to its chosen target instead of flipping between near-tied candidates.</li>
<li>Completing first-run setup now reliably sticks on macOS.</li>
<li>First-run setup is now marked complete as soon as you sign in (rather than only on the final &quot;Open DoorFlow Desktop&quot; button), so quitting during or right after the initial sync no longer restarts the whole first-run wizard on the next launch.</li>
<li>Deleting the template you&#x27;re editing now moves the editor to the next remaining template (instead of leaving the deleted design on the canvas); if no templates remain, it drops to the All People view.</li>
<li>Creating a new card template no longer opens to a blank card - a new template is given a valid default orientation, so it renders immediately instead of only after toggling the front/back orientation.</li>
</ul>]]></description><enclosure url="https://github.com/doorflow/doorflow-app-updates.github.io/releases/download/doorflow-desktop-v1.9.12/DoorFlow%20Desktop-1.9.12%20%2828%29-Setup.exe" sparkle:version="28" sparkle:shortVersionString="1.9.12" length="99537648" type="application/octet-stream" sparkle:edSignature="zdF+nUPFvatvqMqo5fQgfnGxFVNozt3zBQMfdpsE/ZeS15A+LeeiGKa8g+n3fFTQx+IOYuajaMLaQllzvoUuCQ==" sparkle:os="windows"/></item><item><title>Version 1.9.11 (26)</title><pubDate>Tue, 07 Jul 2026 16:29:13 +0000</pubDate><enclosure url="https://github.com/doorflow/doorflow-app-updates.github.io/releases/download/doorflow-desktop-v1.9.11/DoorFlow%20Desktop-1.9.11%20%2826%29-Setup.exe" sparkle:version="26" sparkle:shortVersionString="1.9.11" length="99536144" type="application/octet-stream" sparkle:edSignature="YvShqoagKyAyTtFuhrxIDSxNnruyboZEV0ql/jZcNle+YlCBJf8uKF2UdXn6DJc+OLwtIp2EK8pv6R62Z7BDBw==" sparkle:os="windows"/></item><item>
      <title>Version 1.9.1 (3)</title>
      <pubDate>Mon, 15 Jun 2026 23:05:45 +0000</pubDate>
      <enclosure url="https://github.com/doorflow/release-tool/releases/download/doorflow-desktop-v1.9.1-b3/DoorFlow%20Desktop-1.9.1%20%283%29-Setup.exe" sparkle:version="3" sparkle:shortVersionString="1.9.1" length="99310109" type="application/octet-stream" sparkle:edSignature="oun47jbt0Xbe44+pHG6IJofovpEuuIrQCw5JNIAZdddhM9Cw3kuq0kDsD1IjkcO8MtK42rLIZH/JuYdaMheLDA==" sparkle:os="windows"/>
    </item>
  </channel>
</rss>
